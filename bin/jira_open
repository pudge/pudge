#!/usr/bin/perl
use warnings;
use strict;
use feature ':5.10';

my($id) = @ARGV;
$id //= '';

chomp($id = `jira_id \Q$id\E`);

if ($id && $id =~ /^\w+-\d+$/) {
    system 'open', "https://shiftboard.atlassian.net/browse/$id";
}
